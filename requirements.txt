# ==========================================
# 元氣貝貝 - 雲端部署版 requirements.txt
# 【修復版】解決 Supabase v2 與 aiortc 的非同步事件迴圈衝突
# 核心策略：釘選 anyio 至 3.x 系列，防止 httpx/supabase 拉取 anyio 4.x
# 導致與 aiortc 的 asyncio 後端不相容
# ==========================================

# --- Streamlit 核心 ---
streamlit==1.35.0

# --- WebRTC 視訊串流（陪伴貝貝功能核心）---
# 注意：aiortc 1.9.0 內部依賴 anyio 3.x，必須釘選防止升級
streamlit-webrtc==0.47.6
aiortc==1.9.0
av==12.3.0

# --- 非同步網路衝突修復（最重要的釘選！）---
# anyio 4.x 與 aiortc 的 asyncio backend 不相容，強制使用 3.x
anyio==3.7.1
# httpx 必須與 anyio 3.x 對應
httpx==0.27.0

# --- 電腦視覺核心 ---
# opencv-python-headless：雲端伺服器無 GUI，必須用 headless 版
opencv-python-headless==4.9.0.80
# mediapipe 0.10.14 在某些環境下會拉取 protobuf 4.x 導致衝突
# 釘選 0.10.9 是目前最穩定的版本
mediapipe==0.10.9
numpy==1.26.4

# --- 資料處理 ---
pandas==2.2.2
pillow==10.4.0

# --- 雲端資料庫 ---
# supabase 2.5.3 底層使用 gotrue + postgrest-py
# 配合上方 httpx==0.27.0 和 anyio==3.7.1 可正常運作
supabase==2.5.3
